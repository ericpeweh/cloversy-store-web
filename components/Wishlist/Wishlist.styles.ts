// Dependencies
import { styled } from "@mui/system";

export const WishlistTableContainer = styled("div")({
	width: "145rem"
});

export const WishlistContainer = styled("div")({
	minHeight: "100vh",
	display: "flex",
	alignItems: "center",
	flexDirection: "column"
});
