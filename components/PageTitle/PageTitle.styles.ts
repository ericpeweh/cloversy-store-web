// Dependencies
import { styled } from "@mui/system";

export const TitleContainer = styled("h1")({
	fontSize: "2.6rem",
	fontWeight: 500,
	fontFamily: "var(--font-secondary)",
	margin: "4rem 0 5rem"
});
