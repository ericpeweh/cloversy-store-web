// Dependencies
import { styled } from "@mui/system";

export const ContentDescriptionContainer = styled("p")({
	fontSize: "1.6rem",
	fontWeight: 400,
	lineHeight: "3rem",
	marginBottom: "1.5rem"
});
